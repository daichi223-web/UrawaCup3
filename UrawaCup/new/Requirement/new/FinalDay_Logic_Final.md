# 最終日組み合わせロジック 確定版

---

## 1. 基本構成

```
【順位リーグ】横並び表示
├── リーグ1（上位グループ）  → 会場1
├── リーグ2                  → 会場2
├── リーグ3                  → 会場3
└── リーグ4（下位グループ）  → 会場4

【決勝トーナメント】下に表示
├── 準決勝1: A1位 vs C1位
├── 準決勝2: B1位 vs D1位
├── 3位決定戦
└── 決勝
```

---

## 2. チーム数の変動対応

### 2.1 パターン一覧

| 総チーム数 | グループ構成 | 決勝T | 順位リーグ |
|-----------|-------------|-------|-----------|
| 24 | 4グループ × 6チーム | 4チーム | 5チーム × 4リーグ |
| 20 | 4グループ × 5チーム | 4チーム | 4チーム × 4リーグ |
| 16 | 4グループ × 4チーム | 4チーム | 3チーム × 4リーグ |
| 12 | 4グループ × 3チーム | 4チーム | 2チーム × 4リーグ |

### 2.2 計算ロジック

```typescript
function calculateLeagueSize(totalTeams: number): number {
  const knockoutTeams = 4;  // 決勝T進出は常に4チーム
  const remainingTeams = totalTeams - knockoutTeams;
  const leagueCount = 4;    // 順位リーグは常に4つ
  
  return Math.ceil(remainingTeams / leagueCount);
}

// 例:
// 24チーム → (24-4)/4 = 5チーム/リーグ
// 20チーム → (20-4)/4 = 4チーム/リーグ
// 22チーム → (22-4)/4 = 4.5 → 5チーム（一部4チーム）
```

### 2.3 端数の処理

```
【22チームの場合】
- 決勝T: 4チーム
- 残り: 18チーム

振り分け:
- リーグ1: 5チーム（5〜9位）
- リーグ2: 5チーム（10〜14位）
- リーグ3: 4チーム（15〜18位）
- リーグ4: 4チーム（19〜22位）

※ 上位リーグから多めに配分
```

```typescript
function distributeTeams(teams: Team[], leagueCount: number = 4): Team[][] {
  const leagues: Team[][] = [[], [], [], []];
  
  teams.forEach((team, index) => {
    const leagueIndex = Math.floor(index / Math.ceil(teams.length / leagueCount));
    // 最大4リーグに収める
    const targetLeague = Math.min(leagueIndex, leagueCount - 1);
    leagues[targetLeague].push(team);
  });
  
  return leagues;
}
```

---

## 3. 順位決定ロジック

### 3.1 グループ内順位

```
【決定基準】（優先順）
1. 勝ち点
2. 得失点差
3. 総得点
4. 直接対決
5. ランダム（同成績の場合）
```

### 3.2 同順位間の全体順位

```
【決定基準】（優先順）
1. 勝ち点
2. 得失点差
3. 総得点
4. ランダム（同成績の場合）
```

### 3.3 ランダム決定の実装

```typescript
function resolveStandings(standings: TeamStanding[]): TeamStanding[] {
  // グループ内順位でソート
  const sorted = [...standings].sort((a, b) => {
    // 1. グループ内順位
    if (a.groupRank !== b.groupRank) return a.groupRank - b.groupRank;
    
    // 2. 同順位内で勝ち点
    if (a.points !== b.points) return b.points - a.points;
    
    // 3. 得失点差
    if (a.goalDiff !== b.goalDiff) return b.goalDiff - a.goalDiff;
    
    // 4. 総得点
    if (a.goalsFor !== b.goalsFor) return b.goalsFor - a.goalsFor;
    
    // 5. ランダム
    return Math.random() - 0.5;
  });
  
  // 全体順位を付与
  return sorted.map((team, index) => ({
    ...team,
    overallRank: index + 1,
  }));
}
```

---

## 4. 順位リーグの試合数

| チーム数 | 試合数 | 計算式 |
|---------|--------|--------|
| 5 | 10試合 | 5C2 = 10 |
| 4 | 6試合 | 4C2 = 6 |
| 3 | 3試合 | 3C2 = 3 |
| 2 | 1試合 | 2C2 = 1 |

### 4.1 総当たり対戦順序

```typescript
// 5チームの場合
const matchOrder5 = [
  [0, 1], [2, 3], [0, 4], [1, 2], [3, 4],
  [0, 2], [1, 3], [2, 4], [0, 3], [1, 4]
];

// 4チームの場合
const matchOrder4 = [
  [0, 1], [2, 3], [0, 2], [1, 3], [0, 3], [1, 2]
];

// 3チームの場合
const matchOrder3 = [
  [0, 1], [0, 2], [1, 2]
];

function getMatchOrder(teamCount: number): number[][] {
  switch (teamCount) {
    case 5: return matchOrder5;
    case 4: return matchOrder4;
    case 3: return matchOrder3;
    case 2: return [[0, 1]];
    default: throw new Error(`Unsupported team count: ${teamCount}`);
  }
}
```

---

## 5. 自動生成の完全フロー

```typescript
interface FinalDayGenerateResult {
  trainingLeagues: TrainingLeague[];
  knockout: KnockoutSchedule;
  warnings: Warning[];
}

async function generateFinalDay(tournamentId: number): Promise<FinalDayGenerateResult> {
  const warnings: Warning[] = [];
  
  // ========================================
  // Step 1: 予選結果から順位計算
  // ========================================
  const groupStandings = await calculateGroupStandings(tournamentId);
  
  // ========================================
  // Step 2: 全体順位を決定（ランダムで同成績解決）
  // ========================================
  const overallStandings = resolveStandings(groupStandings);
  
  // ランダム決定があった場合は警告
  const randomDecisions = findRandomDecisions(groupStandings, overallStandings);
  if (randomDecisions.length > 0) {
    warnings.push({
      type: 'random',
      message: `同成績のチームがランダムで順位決定されました: ${randomDecisions.join(', ')}`,
    });
  }
  
  // ========================================
  // Step 3: 決勝T進出チーム（各グループ1位）
  // ========================================
  const knockoutTeams = overallStandings.filter(s => s.groupRank === 1);
  
  // ========================================
  // Step 4: 順位リーグ振り分け
  // ========================================
  const remainingTeams = overallStandings.filter(s => s.groupRank !== 1);
  const leagueTeams = distributeTeams(remainingTeams, 4);
  
  // ========================================
  // Step 5: 会場割り当て
  // ========================================
  const trainingVenues = await getTrainingVenues(tournamentId);
  const finalsVenue = await getFinalsVenue(tournamentId);
  
  // ========================================
  // Step 6: 順位リーグの対戦表生成
  // ========================================
  const trainingLeagues: TrainingLeague[] = leagueTeams.map((teams, index) => {
    const venue = trainingVenues[index];
    const matches = generateRoundRobin(teams);
    const matchesWithTime = assignKickoffTimes(matches, "9:00", 65);
    
    return {
      leagueNumber: index + 1,
      venueId: venue.id,
      venueName: venue.name,
      venueManager: teams[0]?.teamName || '',  // 最上位チームを会場担当に
      teams: teams,
      matches: matchesWithTime,
    };
  });
  
  // ========================================
  // Step 7: 決勝トーナメント生成
  // ========================================
  const knockout = generateKnockout(knockoutTeams, finalsVenue);
  
  return {
    trainingLeagues,
    knockout,
    warnings,
  };
}
```

---

## 6. データ構造

### 6.1 順位リーグ

```typescript
interface TrainingLeague {
  leagueNumber: number;        // 1, 2, 3, 4
  venueId: number;
  venueName: string;
  venueManager: string;        // 会場担当チーム名
  rankRange: string;           // "5〜9位", "10〜14位" など
  teams: LeagueTeam[];
  matches: TrainingMatch[];
}

interface LeagueTeam {
  overallRank: number;         // 全体順位（5, 6, 7...）
  teamId: number;
  teamName: string;
  groupId: string;             // 元のグループ（A, B, C, D）
  groupRank: number;           // グループ内順位
}

interface TrainingMatch {
  id: string;
  order: number;               // 1, 2, 3...
  kickoffTime: string;
  homeTeamId: number;
  awayTeamId: number;
  refereeMain: string;
  refereeAssistant: string;
  // 結果（入力後）
  homeScore: number | null;
  awayScore: number | null;
  status: 'scheduled' | 'completed';
}
```

### 6.2 決勝トーナメント

```typescript
interface KnockoutSchedule {
  venueId: number;
  venueName: string;
  venueManager: string;
  matches: KnockoutMatch[];
}

interface KnockoutMatch {
  id: string;
  matchType: 'semifinal1' | 'semifinal2' | 'third_place' | 'final';
  kickoffTime: string;
  // チーム（準決勝は確定、3決・決勝は未定）
  homeTeamId: number | null;
  awayTeamId: number | null;
  homeSeed: string;            // "A1", "SF1勝者" など
  awaySeed: string;
  refereeMain: string;
  refereeAssistant: string;
  // 結果
  homeScore: number | null;
  awayScore: number | null;
  status: 'scheduled' | 'completed';
}
```

---

## 7. 画面表示

### 7.1 順位リーグ（横並び）

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  【順位リーグ】                                                             │
│                                                                             │
│  ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐ ┌─────────────┐
│  │ 浦和南高G       │ │ 市立浦和高G     │ │ 浦和学院高G     │ │ 武南高G     │
│  │ リーグ1(5〜9位) │ │ リーグ2(10〜14位)│ │ リーグ3(15〜18位)│ │ リーグ4(19〜22位)│
│  ├─────────────────┤ ├─────────────────┤ ├─────────────────┤ ├─────────────┤
│  │[5] 専大北上     │ │[10] 敬愛学園    │ │[15] 富士市立    │ │[19] PHM相模原│
│  │[6] 野辺地西     │ │[11] 旭川実業    │ │[16] 韮崎        │ │[20] アイシン │
│  │[7] 日大明誠     │ │[12] 國學院久我山 │ │[17] 明秀日立    │ │[21] 矢板中央 │
│  │[8] 聖和学園     │ │[13] 健大高崎    │ │[18] 浦和RY      │ │[22] 佐野日大 │
│  │[9] 東海大相模   │ │[14] 日本文理    │ │                 │ │             │
│  ├─────────────────┤ ├─────────────────┤ ├─────────────────┤ ├─────────────┤
│  │ 9:00  [5]-[6]   │ │ 9:00  [10]-[11] │ │ 9:00  [15]-[16] │ │ 9:00  [19]-[20]│
│  │10:05  [7]-[8]   │ │10:05  [12]-[13] │ │10:05  [15]-[17] │ │10:05  [21]-[22]│
│  │11:10  [5]-[9]   │ │11:10  [10]-[14] │ │11:10  [16]-[17] │ │11:10  [19]-[21]│
│  │  ... (10試合)   │ │  ... (10試合)   │ │  ... (6試合)    │ │  ... (6試合) │
│  ├─────────────────┤ ├─────────────────┤ ├─────────────────┤ ├─────────────┤
│  │担当:[専大北上▼] │ │担当:[敬愛学園▼] │ │担当:[富士市立▼] │ │担当:[PHM▼]  │
│  └─────────────────┘ └─────────────────┘ └─────────────────┘ └─────────────┘
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 7.2 決勝トーナメント（下に表示）

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  【決勝トーナメント】 駒場スタジアム                                        │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │                                                                        │ │
│  │  10:00  準決勝1   [A1位▼](浦和南)   vs  [C1位▼](浦和学院)             │ │
│  │                                                                        │ │
│  │  11:10  準決勝2   [B1位▼](市立浦和) vs  [D1位▼](武南)                 │ │
│  │                                                                        │ │
│  │  13:00  3位決定戦  SF1敗者  vs  SF2敗者                                │ │
│  │                                                                        │ │
│  │  14:10  決勝       SF1勝者  vs  SF2勝者                                │ │
│  │                                                                        │ │
│  ├───────────────────────────────────────────────────────────────────────┤ │
│  │  担当: [浦和RY ▼]                                                      │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 8. 警告・通知

### 8.1 自動生成時の警告

```typescript
interface Warning {
  type: 'random' | 'rematch' | 'uneven';
  message: string;
  details?: any;
}

// 例:
{
  type: 'random',
  message: '同成績のチームがランダムで順位決定されました',
  details: ['5位と6位（専大北上、野辺地西）']
}

{
  type: 'rematch',
  message: '予選で対戦済みのチーム同士が順位リーグで再戦します',
  details: [
    { league: 1, teams: ['専大北上', '東海大相模'] }
  ]
}

{
  type: 'uneven',
  message: 'チーム数が均等でないため、リーグによって試合数が異なります',
  details: { league1: 5, league2: 5, league3: 4, league4: 4 }
}
```

### 8.2 画面での表示（上部警告バナー）

```
┌─────────────────────────────────────────────────────────────────┐
│ ⚠️ 警告                                                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ • 同成績のチームがランダムで順位決定されました                  │
│   - 5位と6位（専大北上、野辺地西）                              │
│                                                                 │
│ • 予選で対戦済みのチーム同士が順位リーグで再戦します            │
│   - リーグ1: 専大北上 vs 東海大相模（オレンジ色で表示中）       │
│   → 手動で入れ替えてください                                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 8.3 再戦試合の色分け表示

```
【通常の試合】
┌──────┬─────────────────────────────┬────────────────┐
│ 9:00 │ [専大北上▼] - [野辺地西▼]  │ [当該▼]       │  ← 白背景
└──────┴─────────────────────────────┴────────────────┘

【再戦の試合（予選で対戦済み）】
┌──────┬─────────────────────────────┬────────────────┐
│11:10 │ [専大北上▼] - [東海大相模▼]│ [当該▼]       │  ← オレンジ背景
│      │  ⚠️ 予選で対戦済み          │                │
└──────┴─────────────────────────────┴────────────────┘
```

### 8.4 視覚的フィードバック（CSS）

```css
/* 通常の試合行 */
.match-row {
  background-color: white;
}

/* 再戦の試合行（予選で対戦済み） */
.match-row.rematch {
  background-color: #FFF3E0;  /* オレンジ薄め */
  border-left: 4px solid #FF9800;  /* オレンジ強調 */
}

/* 再戦アイコン */
.rematch-warning {
  color: #E65100;  /* オレンジ濃いめ */
  font-size: 12px;
}

/* ホバー時のツールチップ */
.match-row.rematch:hover::after {
  content: "予選で対戦済みです。入れ替えを検討してください。";
}
```

### 8.5 リーグカードでの表示例

```
┌─────────────────────────────────────────────────────────────────┐
│ 浦和南高G  リーグ1(5〜9位)                        [＋試合追加]  │
├─────────────────────────────────────────────────────────────────┤
│[5] 専大北上  [6] 野辺地西  [7] 日大明誠                        │
│[8] 聖和学園  [9] 東海大相模                                    │
├──────┬─────────────────────────────┬────────────────┬──────────┤
│  KO  │          対 戦              │     審 判      │          │
├──────┼─────────────────────────────┼────────────────┼──────────┤
│ 9:00 │ [専大北上▼] - [野辺地西▼]  │ [当該▼]       │  [🗑️]   │
├──────┼─────────────────────────────┼────────────────┼──────────┤
│10:05 │ [日大明誠▼] - [聖和学園▼]  │ [当該▼]       │  [🗑️]   │
├──────┼─────────────────────────────┼────────────────┼──────────┤
│      │                             │                │          │
│11:10 │ [専大北上▼] - [東海大相模▼]│ [当該▼]       │  [🗑️]   │  ← オレンジ
│      │  ⚠️ 予選A組で対戦済み       │                │          │     背景
│      │                             │                │          │
├──────┼─────────────────────────────┼────────────────┼──────────┤
│12:15 │ [野辺地西▼] - [日大明誠▼]  │ [当該▼]       │  [🗑️]   │
├──────┼─────────────────────────────┼────────────────┼──────────┤
│  ... │           ...               │     ...        │   ...    │
└──────┴─────────────────────────────┴────────────────┴──────────┘
```

### 8.6 再戦チェックのロジック

```typescript
interface RematchInfo {
  matchId: string;
  homeTeamId: number;
  awayTeamId: number;
  homeTeamName: string;
  awayTeamName: string;
  preliminaryMatchInfo: {
    groupId: string;
    matchDate: string;
    score: string;  // "2-1"
  };
}

async function checkRematches(
  tournamentId: number,
  finalDayMatches: FinalDayMatch[]
): Promise<RematchInfo[]> {
  // 予選の全試合を取得
  const preliminaryMatches = await getPreliminaryMatches(tournamentId);
  
  // 対戦済みペアのSetを作成
  const playedPairs = new Set<string>();
  for (const match of preliminaryMatches) {
    const pairKey = [match.homeTeamId, match.awayTeamId].sort().join('-');
    playedPairs.add(pairKey);
  }
  
  // 最終日の試合で再戦をチェック
  const rematches: RematchInfo[] = [];
  for (const match of finalDayMatches) {
    if (!match.homeTeamId || !match.awayTeamId) continue;
    
    const pairKey = [match.homeTeamId, match.awayTeamId].sort().join('-');
    if (playedPairs.has(pairKey)) {
      // 予選での対戦情報を取得
      const prelimMatch = preliminaryMatches.find(pm => 
        (pm.homeTeamId === match.homeTeamId && pm.awayTeamId === match.awayTeamId) ||
        (pm.homeTeamId === match.awayTeamId && pm.awayTeamId === match.homeTeamId)
      );
      
      rematches.push({
        matchId: match.id,
        homeTeamId: match.homeTeamId,
        awayTeamId: match.awayTeamId,
        homeTeamName: match.homeTeamName,
        awayTeamName: match.awayTeamName,
        preliminaryMatchInfo: {
          groupId: prelimMatch.groupId,
          matchDate: prelimMatch.matchDate,
          score: `${prelimMatch.homeScore}-${prelimMatch.awayScore}`,
        },
      });
    }
  }
  
  return rematches;
}
```

### 8.7 Reactコンポーネント例

```tsx
interface MatchRowProps {
  match: FinalDayMatch;
  isRematch: boolean;
  rematchInfo?: RematchInfo;
}

function MatchRow({ match, isRematch, rematchInfo }: MatchRowProps) {
  return (
    <tr className={`match-row ${isRematch ? 'rematch' : ''}`}>
      <td>
        <TimeSelect value={match.kickoffTime} />
      </td>
      <td>
        <div className="flex items-center gap-2">
          <TeamSelect value={match.homeTeamId} />
          <span>-</span>
          <TeamSelect value={match.awayTeamId} />
        </div>
        {isRematch && (
          <div className="rematch-warning flex items-center gap-1 mt-1">
            <WarningIcon className="w-4 h-4" />
            <span>
              予選{rematchInfo.preliminaryMatchInfo.groupId}組で対戦済み
              （{rematchInfo.preliminaryMatchInfo.score}）
            </span>
          </div>
        )}
      </td>
      <td>
        <RefereeSelect value={match.refereeMain} />
      </td>
      <td>
        <DeleteButton matchId={match.id} />
      </td>
    </tr>
  );
}
```

---

追加した内容
8.8　再戦試合の視覚的警告
【通常】白背景
┌──────┬─────────────────────────────┐
│ 9:00 │ [専大北上▼] - [野辺地西▼]  │
└──────┴─────────────────────────────┘

【再戦】オレンジ背景 + 左にオレンジのバー
┌──────┬─────────────────────────────┐
│11:10 │ [専大北上▼] - [東海大相模▼]│  ← オレンジ背景
│      │  ⚠️ 予選A組で対戦済み       │
└──────┴─────────────────────────────┘
実装内容

オレンジ背景 + 左ボーダーで強調
「予選◯組で対戦済み（スコア）」を表示
予選全試合との突合チェックロジック


## 9. チェックリスト

### ロジック
- [ ] グループ内順位計算
- [ ] 同順位間の全体順位計算
- [ ] 同成績時のランダム決定
- [ ] チーム数に応じた振り分け
- [ ] 端数チームの処理
- [ ] 総当たり対戦表生成
- [ ] キックオフ時間自動割り当て

### 画面
- [ ] 順位リーグ4つ横並び表示
- [ ] 各リーグの参加チーム表示
- [ ] 各リーグの試合一覧表示
- [ ] 決勝トーナメント表示
- [ ] 警告メッセージ表示

### 編集
- [ ] チームドロップダウン選択
- [ ] クリック入れ替え
- [ ] 時間変更
- [ ] 審判変更
- [ ] 会場担当変更
- [ ] 試合追加/削除
